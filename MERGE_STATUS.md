# Merge status

- Current branch: `work`.
- HEAD commit: `6d6262c` ("Improve tarot UI and bundle sample deck data").
- Working tree is clean (no unmerged paths).
- Recent history is linear; no pending merge commits.

To re-check locally:
1. `git status -sb`
2. `git log --graph --oneline --decorate -5`
